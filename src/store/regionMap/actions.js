import { createAction } from 'redux-actions';

export const toggleGridRotation = createAction('toggle grid rotation');
export const toggleStarColor = createAction('toggle star color');
export const triggerReRender = createAction('trigger re-render');
